import React from 'react';
import NewBookingFlowScreen from './NewBookingFlowScreen';

const UnifiedBookingScreen: React.FC<any> = (props) => {
  return <NewBookingFlowScreen {...props} />;
};

export default UnifiedBookingScreen;
